---
import mePhoto from '@/assets/me/me.jpg';
import BaseHead from '@/components/BaseHead.astro';
import Footer from '@/components/Footer.astro';
import Header from '@/components/Header.astro';
import Icon from '@/components/Icon.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '@/consts';
import { Image } from 'astro:assets';

// Curated core skills
const coreSkills = [
	{ name: 'Python', category: 'language' },
	{ name: 'Rust', category: 'language' },
	{ name: 'TypeScript', category: 'language' },
	{ name: 'System Design', category: '' },
	{ name: 'AWS', category: 'cloud' },
	{ name: 'Kubernetes', category: 'cloud' },
	{ name: 'Data Platforms', category: 'focus' },
	{ name: 'MLOps', category: 'focus' },
	{ name: 'Apache Spark', category: 'data' },
	{ name: 'PostgreSQL', category: 'data' },
	{ name: 'Terraform', category: 'infra' },
];
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />

		<!-- Hero Section with Cinematic Layout -->
		<section class="hero-main">
			<!-- Vignette overlay for cinematic depth -->
			<div class="hero-vignette"></div>

			<div class="hero-grid">
				<!-- Left: Identity & Impact -->
				<div class="hero-content">
					<div class="location-badge glitch-wrapper">
						<span class="location-from">🇪🇸 Spain</span>
						<Icon name="mdi:arrow-right" size={16} />
						<span class="location-to">🇯🇵 Japan</span>
					</div>

					<h1 class="hero-name">
						<span class="name-line">Adrian</span>
						<span class="name-line">Villanueva</span>
						<span class="name-line crimson">Martinez</span>
					</h1>

					<div class="hero-tagline">
						<p class="tagline-main">
							High-impact engineer building <span
								class="highlight-cyan"
								>cloud-native data platforms</span
							>
							that scale globally and drive <span
								class="highlight-crimson">massive adoption</span
							>.
						</p>
						<p class="tagline-sub">
							Software & Data • MLOps • Platform Architecture
						</p>
					</div>

					<div class="cta-group">
						<a href="/about" class="cta-primary">
							<Icon name="mdi:briefcase-outline" size={20} />
							<span>About</span>
							<div class="cta-glow"></div>
						</a>
						<a href="/contact" class="cta-secondary">
							<Icon name="mdi:email-fast-outline" size={20} />
							<span>Let's Talk</span>
						</a>
					</div>
				</div>

				<!-- Right: Cinematic Photo -->
				<div class="hero-visual">
					<div class="photo-frame">
						<Image
							src={mePhoto}
							alt="Adrian Villanueva Martinez"
							class="hero-photo"
							loading="eager"
						/>
						<div class="photo-overlay"></div>
						<div class="photo-border"></div>
					</div>
					<div class="visual-accent"></div>
				</div>
			</div>

			<!-- Floating Stats Bar -->
			<div class="stats-bar">
				<div class="stat-item">
					<Icon name="mdi:code-braces" size={24} />
					<div class="stat-content">
						<span class="stat-value">Constant</span>
						<span class="stat-label">Learning</span>
					</div>
				</div>
				<div class="stat-divider"></div>
				<div class="stat-item">
					<Icon name="mdi:cloud-outline" size={24} />
					<div class="stat-content">
						<span class="stat-value">Cloud</span>
						<span class="stat-label">Native</span>
					</div>
				</div>
				<div class="stat-divider"></div>
				<div class="stat-item">
					<Icon name="mdi:chart-line" size={24} />
					<div class="stat-content">
						<span class="stat-value">Scale</span>
						<span class="stat-label">Focused</span>
					</div>
				</div>
			</div>
		</section>

		<!-- Core Skills Section -->
		<section class="skills-showcase">
			<div class="section-container">
				<h2 class="section-heading reveal-on-scroll">
					<span class="heading-line"></span>
					<span class="heading-text">Core Expertise</span>
					<span class="heading-line"></span>
				</h2>

				<div class="skills-grid-main">
					{
						coreSkills.map((skill, index) => (
							<div
								class={`skill-card skill-${skill.category} reveal-on-scroll`}
								style={`--reveal-delay: ${index * 0.05}s`}
							>
								<span class="skill-label">{skill.name}</span>
								<div class="skill-glow" />
							</div>
						))
					}
				</div>

				<div class="skills-footer reveal-on-scroll">
					<a href="/resume" class="link-subtle">
						<span>Full technical stack</span>
						<Icon name="mdi:arrow-right" size={16} />
					</a>
				</div>
			</div>
		</section>

		<Footer />

		<!-- Performance-optimized scroll reveal script -->
		<script>
			// Use Intersection Observer for optimal performance
			const observerOptions = {
				root: null,
				rootMargin: '0px 0px -100px 0px', // Trigger slightly before element enters viewport
				threshold: 0.1,
			};

			const observer = new IntersectionObserver((entries) => {
				entries.forEach((entry) => {
					if (entry.isIntersecting) {
						entry.target.classList.add('revealed');
						// Unobserve after revealing for performance
						observer.unobserve(entry.target);
					}
				});
			}, observerOptions);

			// Observe all elements with reveal class
			document.querySelectorAll('.reveal-on-scroll').forEach((el) => {
				observer.observe(el);
			});
		</script>
	</body>
</html>

<style>
	/* ========================================
	   DARK BRUTALIST-SYNTHWAVE AESTHETIC
	   Fast, Modern, Intense
	======================================== */

	* {
		box-sizing: border-box;
	}

	/* ========================================
	   HERO SECTION - Cinematic & Bold
	======================================== */

	.hero-main {
		min-height: 100vh;
		background: var(--bg-primary);
		position: relative;
		overflow: hidden;
		padding: 6rem 2rem 4rem;
		display: flex;
		flex-direction: column;
		justify-content: center;
		isolation: isolate;
	}

	/* Animated background grid - More visible */
	.hero-main::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background-image: linear-gradient(
				rgba(0, 255, 255, 0.06) 1px,
				transparent 1px
			),
			linear-gradient(90deg, rgba(0, 255, 255, 0.06) 1px, transparent 1px);
		background-size: 50px 50px;
		animation: grid-move 20s linear infinite;
		pointer-events: none;
		z-index: 0;
	}

	/* Synthwave scanline effect */
	.hero-main::after {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: linear-gradient(
			transparent 50%,
			rgba(0, 255, 255, 0.03) 50%
		);
		background-size: 100% 4px;
		animation: scanline 8s linear infinite;
		pointer-events: none;
		z-index: 0;
	}

	@keyframes grid-move {
		0% {
			transform: translate(0, 0);
		}
		100% {
			transform: translate(50px, 50px);
		}
	}

	@keyframes scanline {
		0% {
			transform: translateY(-100%);
		}
		100% {
			transform: translateY(100%);
		}
	}

	/* Cinematic vignette overlay - Pure CSS, zero performance cost */
	.hero-vignette {
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: radial-gradient(
			ellipse at center,
			transparent 40%,
			rgba(0, 0, 0, 0.4) 100%
		);
		pointer-events: none;
		z-index: 1;
	}

	.hero-grid {
		max-width: 1400px;
		margin: 0 auto;
		display: grid;
		grid-template-columns: 1.2fr 1fr;
		gap: 4rem;
		align-items: center;
		position: relative;
		z-index: 2; /* Above vignette */
	}

	/* Left Side - Content */
	.hero-content {
		animation: fade-in-up 0.8s ease-out;
	}

	@keyframes fade-in-up {
		from {
			opacity: 0;
			transform: translateY(30px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	.location-badge {
		display: inline-flex;
		align-items: center;
		gap: 0.75rem;
		padding: 0.5rem 1rem;
		background: var(--bg-secondary);
		border: 2px solid var(--bg-tertiary);
		margin-bottom: 2rem;
		font-family: 'Orbitron', monospace;
		font-size: 0.85rem;
		font-weight: 600;
		color: var(--text-secondary);
		transition: all 0.3s ease;
	}

	.location-badge:hover {
		border-color: var(--accent-cyan);
		box-shadow: 0 0 20px rgba(0, 255, 255, 0.2);
	}

	.location-from {
		color: var(--text-secondary);
	}

	.location-to {
		color: var(--accent-cyan);
		text-shadow: 0 0 10px var(--accent-cyan-glow);
	}

	.location-badge svg {
		color: var(--accent-cyan);
	}

	.hero-name {
		font-family: 'Orbitron', monospace;
		font-weight: 900;
		font-size: clamp(3rem, 8vw, 6rem);
		line-height: 0.9;
		margin-bottom: 2rem;
		letter-spacing: -0.03em;
		cursor: default;
	}

	.name-line {
		display: block;
		color: var(--text-primary);
		text-transform: uppercase;
		position: relative;
		transition: all 0.3s ease;
	}

	/* Enhanced glitch effect on hover */
	.hero-name:hover .name-line {
		animation: glitch-intense 0.3s ease-in-out;
	}

	.name-line.crimson {
		color: var(--accent-crimson);
		text-shadow:
			0 0 20px var(--accent-crimson-glow),
			2px 2px 0 rgba(220, 20, 60, 0.3);
	}

	/* Intense glitch animation - triggered on hover */
	@keyframes glitch-intense {
		0% {
			transform: translate(0);
			text-shadow: 0 0 0 transparent;
		}
		20% {
			transform: translate(-3px, 2px);
			text-shadow:
				3px -2px 0 var(--accent-cyan),
				-3px 2px 0 var(--accent-crimson);
		}
		40% {
			transform: translate(2px, -3px);
			text-shadow:
				-2px 3px 0 var(--accent-cyan),
				2px -3px 0 var(--accent-crimson);
		}
		60% {
			transform: translate(-2px, -2px);
			text-shadow:
				2px 2px 0 var(--accent-cyan),
				-2px -2px 0 var(--accent-crimson);
		}
		80% {
			transform: translate(3px, 3px);
			text-shadow:
				-3px -3px 0 var(--accent-cyan),
				3px 3px 0 var(--accent-crimson);
		}
		100% {
			transform: translate(0);
			text-shadow: 0 0 0 transparent;
		}
	}

	.hero-tagline {
		margin-bottom: 3rem;
	}

	.tagline-main {
		font-size: clamp(1.2rem, 2vw, 1.5rem);
		line-height: 1.6;
		color: var(--text-secondary);
		margin-bottom: 1rem;
		font-weight: 400;
	}

	.highlight-cyan {
		color: var(--accent-cyan);
		font-weight: 600;
		text-shadow: 0 0 10px var(--accent-cyan-glow);
	}

	.highlight-crimson {
		color: var(--accent-crimson);
		font-weight: 600;
		text-shadow: 0 0 10px var(--accent-crimson-glow);
	}

	.tagline-sub {
		font-family: 'Orbitron', monospace;
		font-size: 0.9rem;
		font-weight: 600;
		color: var(--text-muted);
		text-transform: uppercase;
		letter-spacing: 0.15em;
	}

	.cta-group {
		display: flex;
		gap: 1.5rem;
		flex-wrap: wrap;
	}

	.cta-primary {
		position: relative;
		display: inline-flex;
		align-items: center;
		gap: 0.75rem;
		padding: 1.2rem 2.5rem;
		background: var(--accent-cyan);
		color: var(--bg-primary);
		font-family: 'Orbitron', monospace;
		font-weight: 700;
		font-size: 0.95rem;
		text-transform: uppercase;
		letter-spacing: 0.1em;
		text-decoration: none;
		border: 3px solid var(--accent-cyan);
		overflow: hidden;
		transition: all 0.3s ease;
		animation: neon-pulse-cyan 3s ease-in-out infinite;
	}

	/* Subtle neon pulse animation */
	@keyframes neon-pulse-cyan {
		0%,
		100% {
			box-shadow: 0 0 10px rgba(0, 255, 255, 0.3);
		}
		50% {
			box-shadow:
				0 0 20px rgba(0, 255, 255, 0.6),
				0 0 30px rgba(0, 255, 255, 0.3);
		}
	}

	.cta-primary::before {
		content: '';
		position: absolute;
		top: 0;
		left: -100%;
		width: 100%;
		height: 100%;
		background: linear-gradient(
			90deg,
			transparent,
			rgba(255, 255, 255, 0.3),
			transparent
		);
		transition: left 0.5s;
	}

	.cta-primary:hover::before {
		left: 100%;
	}

	.cta-primary:hover {
		transform: translate(-3px, -3px);
		box-shadow: 6px 6px 0 var(--accent-cyan);
		animation: none; /* Stop pulse on hover */
	}

	.cta-primary svg {
		width: 20px;
		height: 20px;
	}

	.cta-secondary {
		display: inline-flex;
		align-items: center;
		gap: 0.75rem;
		padding: 1.2rem 2.5rem;
		background: transparent;
		color: var(--text-primary);
		font-family: 'Orbitron', monospace;
		font-weight: 700;
		font-size: 0.95rem;
		text-transform: uppercase;
		letter-spacing: 0.1em;
		text-decoration: none;
		border: 3px solid var(--accent-crimson);
		transition: all 0.3s ease;
		animation: neon-pulse-crimson 3s ease-in-out infinite;
		animation-delay: 1.5s; /* Offset from primary button */
	}

	/* Subtle neon pulse animation for crimson */
	@keyframes neon-pulse-crimson {
		0%,
		100% {
			box-shadow: 0 0 10px rgba(220, 20, 60, 0.3);
		}
		50% {
			box-shadow:
				0 0 20px rgba(220, 20, 60, 0.6),
				0 0 30px rgba(220, 20, 60, 0.3);
		}
	}

	.cta-secondary:hover {
		background: var(--accent-crimson);
		color: var(--bg-primary);
		transform: translate(-3px, -3px);
		box-shadow: 6px 6px 0 var(--accent-crimson);
		animation: none; /* Stop pulse on hover */
	}

	.cta-secondary svg {
		width: 20px;
		height: 20px;
	}

	/* Right Side - Photo */
	.hero-visual {
		position: relative;
		animation: fade-in-up 0.8s ease-out 0.2s both;
	}

	.photo-frame {
		position: relative;
		aspect-ratio: 3/4;
		max-width: 450px;
		margin: 0 auto;
		overflow: hidden;
	}

	.hero-photo {
		width: 100%;
		height: 100%;
		object-fit: cover;
		filter: grayscale(0.3) contrast(1.1);
		transition: all 0.5s ease;
	}

	.photo-frame:hover .hero-photo {
		filter: grayscale(0) contrast(1.2);
		transform: scale(1.05);
	}

	.photo-overlay {
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: linear-gradient(
			135deg,
			rgba(0, 255, 255, 0.1) 0%,
			transparent 50%,
			rgba(220, 20, 60, 0.1) 100%
		);
		pointer-events: none;
		mix-blend-mode: overlay;
	}

	.photo-border {
		position: absolute;
		top: -10px;
		left: -10px;
		right: 10px;
		bottom: 10px;
		border: 3px solid var(--accent-cyan);
		pointer-events: none;
		transition: all 0.3s ease;
	}

	.photo-frame:hover .photo-border {
		top: -15px;
		left: -15px;
		right: 15px;
		bottom: 15px;
		box-shadow: 0 0 30px rgba(0, 255, 255, 0.3);
	}

	.visual-accent {
		position: absolute;
		bottom: -20px;
		right: -20px;
		width: 200px;
		height: 200px;
		background: radial-gradient(
			circle,
			var(--accent-crimson-glow) 0%,
			transparent 70%
		);
		pointer-events: none;
		animation: pulse 3s ease-in-out infinite;
	}

	@keyframes pulse {
		0%,
		100% {
			opacity: 0.3;
			transform: scale(1);
		}
		50% {
			opacity: 0.6;
			transform: scale(1.1);
		}
	}

	/* Stats Bar */
	.stats-bar {
		display: flex;
		justify-content: center;
		align-items: center;
		gap: 3rem;
		max-width: 800px;
		margin: 4rem auto 0;
		padding: 1.5rem 2rem;
		background: var(--bg-secondary);
		border: 2px solid var(--bg-tertiary);
		border-left: 4px solid var(--accent-cyan);
		animation: fade-in-up 0.8s ease-out 0.4s both;
	}

	.stat-item {
		display: flex;
		align-items: center;
		gap: 1rem;
	}

	.stat-item svg {
		color: var(--accent-cyan);
		width: 24px;
		height: 24px;
	}

	.stat-content {
		display: flex;
		flex-direction: column;
	}

	.stat-value {
		font-family: 'Orbitron', monospace;
		font-weight: 900;
		font-size: 1.2rem;
		color: var(--text-primary);
		line-height: 1;
	}

	.stat-label {
		font-size: 0.75rem;
		color: var(--text-muted);
		text-transform: uppercase;
		letter-spacing: 0.1em;
	}

	.stat-divider {
		width: 2px;
		height: 40px;
		background: var(--bg-tertiary);
	}

	/* ========================================
	   SECTION STYLES - Consistent Structure
	======================================== */

	.section-container {
		max-width: 1400px;
		margin: 0 auto;
		padding: 6rem 2rem;
		position: relative;
		z-index: 1; /* Ensure content appears above background pattern */
	}

	.section-heading {
		display: flex;
		align-items: center;
		justify-content: center;
		gap: 2rem;
		margin-bottom: 4rem;
	}

	.heading-line {
		flex: 1;
		height: 2px;
		max-width: 200px;
		background: linear-gradient(
			90deg,
			transparent,
			var(--accent-cyan),
			transparent
		);
	}

	.heading-text {
		font-family: 'Orbitron', monospace;
		font-weight: 900;
		font-size: clamp(1.8rem, 4vw, 3rem);
		color: var(--text-primary);
		text-transform: uppercase;
		letter-spacing: 0.05em;
		white-space: nowrap;
	}

	/* ========================================
	   SKILLS SECTION
	======================================== */

	.skills-showcase {
		background: var(--bg-secondary);
		position: relative;
		isolation: isolate; /* Create stacking context */
	}

	/* Subtle dot pattern background - GPU accelerated */
	.skills-showcase::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background-image: radial-gradient(
			circle at center,
			rgba(0, 255, 255, 0.08) 1.5px,
			transparent 1.5px
		);
		background-size: 24px 24px;
		pointer-events: none;
		z-index: 0;
		opacity: 1;
	}

	/* Top accent line */
	.skills-showcase::after {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 2px;
		background: linear-gradient(
			90deg,
			transparent,
			var(--accent-cyan),
			transparent
		);
		z-index: 1;
	}

	.skills-grid-main {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
		gap: 1.5rem;
		margin-bottom: 3rem;
	}

	.skill-card {
		position: relative;
		padding: 2rem 1.5rem;
		background: var(--bg-primary);
		border: 2px solid var(--bg-tertiary);
		text-align: center;
		transition: all 0.3s ease;
		overflow: hidden;
	}

	.skill-card::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 3px;
		background: var(--accent-cyan);
		transform: scaleX(0);
		transition: transform 0.3s ease;
	}

	.skill-card:hover::before {
		transform: scaleX(1);
	}

	.skill-card:hover {
		border-color: var(--accent-cyan);
		transform: translateY(-5px);
		box-shadow: 0 10px 30px rgba(0, 255, 255, 0.2);
	}

	.skill-label {
		font-family: 'Orbitron', monospace;
		font-weight: 700;
		font-size: 1rem;
		color: var(--text-primary);
		text-transform: uppercase;
		letter-spacing: 0.05em;
		transition: color 0.3s ease;
	}

	.skill-card:hover .skill-label {
		color: var(--accent-cyan);
		text-shadow: 0 0 10px var(--accent-cyan-glow);
	}

	/* Skill category colors */
	.skill-language:hover {
		border-color: var(--accent-cyan);
	}

	.skill-cloud:hover {
		border-color: var(--accent-crimson);
	}

	.skill-cloud:hover::before {
		background: var(--accent-crimson);
	}

	.skill-cloud:hover .skill-label {
		color: var(--accent-crimson);
	}

	.skill-focus:hover {
		border-color: #00ff00;
	}

	.skill-focus:hover::before {
		background: #00ff00;
	}

	.skill-focus:hover .skill-label {
		color: #00ff00;
	}

	.skills-footer {
		text-align: center;
	}

	.link-subtle {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		font-family: 'Orbitron', monospace;
		font-size: 0.9rem;
		font-weight: 600;
		color: var(--text-secondary);
		text-decoration: none;
		text-transform: uppercase;
		letter-spacing: 0.1em;
		transition: all 0.3s ease;
	}

	.link-subtle:hover {
		color: var(--accent-cyan);
		gap: 0.75rem;
	}

	.link-subtle svg {
		width: 16px;
		height: 16px;
	}

	/* ========================================
	   RESPONSIVE DESIGN
	======================================== */

	@media (max-width: 1024px) {
		.hero-grid {
			grid-template-columns: 1fr;
			gap: 3rem;
		}

		.hero-visual {
			max-width: 400px;
			margin: 0 auto;
		}

		.section-container {
			padding: 4rem 2rem;
		}

		.project-card {
			grid-template-columns: 60px 1fr;
			gap: 1.5rem;
			padding: 2rem;
		}

		.project-number {
			font-size: 2.5rem;
		}

		.project-title {
			font-size: 1.5rem;
		}
	}

	@media (max-width: 768px) {
		.hero-main {
			padding: 4rem 1.5rem 3rem;
		}

		.hero-name {
			font-size: clamp(2rem, 10vw, 3.5rem);
		}

		.tagline-main {
			font-size: 1.1rem;
		}

		.cta-group {
			flex-direction: column;
			gap: 1rem;
		}

		.cta-primary,
		.cta-secondary {
			width: 100%;
			justify-content: center;
			padding: 1rem 2rem;
		}

		.stats-bar {
			flex-direction: column;
			gap: 1.5rem;
			padding: 1.5rem 1rem;
		}

		.stat-divider {
			width: 60px;
			height: 2px;
		}

		.section-container {
			padding: 3rem 1.5rem;
		}

		.section-heading {
			gap: 1rem;
		}

		.heading-line {
			max-width: 50px;
		}

		.heading-text {
			font-size: 1.5rem;
		}

		.skills-grid-main {
			grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
			gap: 1rem;
		}

		.skill-card {
			padding: 1.5rem 1rem;
		}

		.skill-label {
			font-size: 0.85rem;
		}

		.project-card {
			grid-template-columns: 1fr;
			gap: 1rem;
			padding: 1.5rem;
		}

		.project-number {
			font-size: 2rem;
		}

		.project-title {
			font-size: 1.3rem;
		}

		.blog-grid {
			grid-template-columns: 1fr;
		}
	}

	@media (max-width: 480px) {
		.hero-main {
			padding: 3rem 1rem 2rem;
		}

		.location-badge {
			font-size: 0.75rem;
			padding: 0.4rem 0.75rem;
			gap: 0.5rem;
		}

		.hero-name {
			font-size: 2rem;
			margin-bottom: 1.5rem;
		}

		.tagline-main {
			font-size: 1rem;
		}

		.tagline-sub {
			font-size: 0.8rem;
		}

		.cta-primary,
		.cta-secondary {
			font-size: 0.85rem;
			padding: 0.9rem 1.5rem;
		}

		.photo-frame {
			max-width: 300px;
		}

		.section-container {
			padding: 2.5rem 1rem;
		}

		.section-heading {
			margin-bottom: 2.5rem;
		}

		.heading-text {
			font-size: 1.3rem;
		}

		.skills-grid-main {
			grid-template-columns: repeat(2, 1fr);
		}

		.project-card {
			padding: 1.25rem;
		}

		.project-title {
			font-size: 1.2rem;
		}

		.project-desc {
			font-size: 0.9rem;
		}

		.blog-card {
			padding: 1.5rem;
		}

		.blog-title {
			font-size: 1.1rem;
		}
	}

	/* ========================================
	   SCROLL REVEAL ANIMATIONS - Performance Optimized
	======================================== */

	/* Initial state - hidden */
	.reveal-on-scroll {
		opacity: 0;
		transform: translateY(30px);
		transition:
			opacity 0.6s ease-out,
			transform 0.6s ease-out;
		transition-delay: var(--reveal-delay, 0s);
		will-change: opacity, transform;
	}

	/* Revealed state - visible */
	.reveal-on-scroll.revealed {
		opacity: 1;
		transform: translateY(0);
	}

	/* Remove will-change after animation completes for better performance */
	.reveal-on-scroll.revealed {
		animation: remove-will-change 0.1s 0.7s forwards;
	}

	@keyframes remove-will-change {
		to {
			will-change: auto;
		}
	}

	/* ========================================
	   ACCESSIBILITY
	======================================== */

	@media (prefers-reduced-motion: reduce) {
		*,
		*::before,
		*::after {
			animation-duration: 0.01ms !important;
			animation-iteration-count: 1 !important;
			transition-duration: 0.01ms !important;
		}
	}
</style>
